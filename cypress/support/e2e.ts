/**
 * This file is loaded automatically before every Cypress E2E spec.
 * It is the right place to add global behaviour (custom commands,
 * uncaught exception handlers, etc.).
 */

import './commands'
